services:
  - type: web
    name: quotes-rasa-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: rasa run --enable-api --cors "*" --port $PORT
    plan: free
    pythonVersion: 3.8.10